name: curator-evolver
version: "0.5.0"
description: Evidence-driven skill evolution plugin with reports, proposals, candidate search, and guarded apply.
author: pingchesu
kind: standalone
provides_tools:
  - curator_evidence_report
provides_hooks:
  - post_tool_call
  - post_llm_call
  - on_session_end
