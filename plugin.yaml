name: curator-evolver
version: "0.10.0"
description: Plug-and-play evidence-driven skill evolution plugin with one-command bootstrap, provenance-safe autorun, historical session backfill, optional semantic/rerank ordering, and guarded apply.
author: pingchesu
kind: standalone
provides_tools:
  - curator_evidence_report
provides_hooks:
  - post_tool_call
  - post_llm_call
  - on_session_end
